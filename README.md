# Solved-Tasks
   #### Convert a Boolean to a String
```javascript
function booleanToString(b){
  if (b === true) return String(b);
  return String(b);
}
```
  #### Sum The Strings
 ```javascript
function sumStr(a,b) {
  return String(+a + +b);
}
```