# Solved-Tasks
   #### Count Odd Numbers below n
```javascript
function oddCount(n){
  return Math.floor(n / 2);
}
```
  #### Century From Year
 ```javascript
function century(year) {
  return Math.floor((year - 1) / 100 + 1)
}
```
 